server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  location / { try_files $uri /index.html; }
  location ~* \.(js|css|svg|woff2)$ { expires 7d; add_header Cache-Control "public"; }
}
